# 배포 체크리스트 — 감염병 연대기 플랫폼

`portal/` 디렉터리 하나가 **자체 포함 정적 사이트**다(308KB · 6페이지 · 외부 의존은 Google Fonts뿐).
어떤 정적 호스팅에 올려도 그대로 동작한다. 아래 셋 중 하나만 하면 된다.

## 배포 전 상태 (준비 완료 ✅)

- [x] `portal/index.html` — 허브 (연대기·검사기·법령70년·인플루엔자100년·상황판 내비)
- [x] `portal/chronicle/` — 감염병 연대기 v1 (91종 타임머신)
- [x] `portal/checker/` — 단절점 검사기 v3 (81항목)
- [x] `portal/law/` `portal/flu/` `portal/daily/` — 기존 3종
- [x] **모든 링크 상대경로** — 하위 경로 호스팅(예: `/disease/`)에서도 동작
- [x] **claude.ai 의존 0건** — 포털 내부 페이지끼리 상대 링크로 연결
- [x] QA: 6페이지 전부 JS 오류 0 · 깨진 링크 0 · 라이트/다크 정상 (헤드리스 검증)
- [x] `.github/workflows/pages.yml` — GitHub Pages 자동 배포 워크플로
- [x] `netlify.toml` — `publish = "portal"` 설정

## 방법 A — GitHub Pages (권장 · 무료 · 클릭 2번)

워크플로는 이미 저장소에 있다. 저장소 설정만 켜면 된다:

1. https://github.com/soonryu74/disease → **Settings → Pages**
2. **Source**를 `GitHub Actions`로 선택
3. 끝. 다음 푸시(또는 Actions 탭에서 `Deploy portal to GitHub Pages` → Run workflow)에 배포된다
4. 주소: `https://soonryu74.github.io/disease/`

> 워크플로는 현재 기본 브랜치(`claude/infectious-disease-resources-org-kbjcfz`)의 `portal/**` 변경 시 자동 실행된다. 브랜치를 바꾸면 `pages.yml`의 branches만 고치면 된다.
> ⚠️ 저장소가 **private이면** GitHub Pages는 유료 플랜에서만 된다. 공개 전환 여부는 결정 사항(B3).

## 방법 B — Netlify

`netlify.toml`이 이미 `publish = "portal"`로 설정돼 있다.

- 계정 크레딧 문제(이전 실패 원인)가 풀렸다면: Netlify 대시보드에서 사이트 `infection-dashboard-kr` → Deploys → **Trigger deploy**
- 또는 가장 단순하게: https://app.netlify.com/drop 에 `portal/` 폴더를 끌어다 놓기 (계정 무관, 즉시 URL)

## 방법 C — Vercel

이전 실패 원인은 claude.ai Vercel 커넥터가 특정 프로젝트로 스코프된 것.
Vercel 대시보드에서 직접: **Add New → Project → soonryu74/disease** 가져오기 → **Root Directory = `portal`**, Framework = Other. 끝.

## 배포 후 확인 (1분)

- [ ] `/` 허브가 뜨고 상단 내비에 📜연대기·🩺검사기가 보인다
- [ ] `/chronicle/` 매독 선택 → 슬라이더를 2021→2024로 옮기면 제4급→제3급으로 바뀐다
- [ ] `/checker/` 매독 2023↔2024 → 🔴 "표본감시 구간" 판정이 나온다
- [ ] 휴대폰에서 한 번 열어본다 (반응형 확인됨, 실기기 재확인)

## 갱신 루틴

```bash
python3 08_비교가능성_검사기/build_rules.py     # 검사기 데이터
python3 09_감염병연대기/build_chronicle.py       # 연대기 데이터+페이지
python3 scripts/build_portal.py                  # 포털 번들 재조립
git add -A && git commit && git push             # → Pages 자동 재배포
```

> 연 1회(6월 말) 「감염병 신고 현황 연보」 신간이 나오면: CSV에 열 추가 → `BREAKS/GAPS/PARTIAL`에 그해 각주 등재 → 위 3줄.
